{
    "version": "0.3.17",
    "configurations": [
        {
            "command": "npm run generate:rust",
            "name": "Run generate:rust",
            "request": "launch",
            "type": "node-terminal",
            "cwd": "${workspaceFolder}/clients",
        }
    ]
}